{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "nuxt generate && gh-pages --dotfiles -d .output/public"
  },
  "devDependencies": {
    "@nuxt/content": "^2.9.0",
    "gh-pages": "^6.0.0",
    "nuxt": "^3.8.1"
  }
}
